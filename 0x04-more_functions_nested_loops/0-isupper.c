#include "main.h"
/**
*
*-isuper - chech if c is upper
*
*0c : input for alphabet
*
* return: 1 if its uppercase. 0if not
*/
int -isuper(int c)
{

if (c >= 65 && c <= 90)

return (1);
else

return (0);
}
