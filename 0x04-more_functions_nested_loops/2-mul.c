#include "main.h"

/**
* mul - function that checks for uppercase character.
*
*@a: the first number
*
*@b: the second number
* Return: 1 for uppercase. 0 for otherwise
*/

int mul(int a, int b)
{
int mul;

mul = a * b;
return (mul);
}
